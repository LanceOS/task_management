services:
  postgres:
    image: postgres:latest
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: user     
      POSTGRES_PASSWORD: user 
      POSTGRES_DB: my_db          
    volumes:
      - ./database:/var/lib/postgresql/data
  
